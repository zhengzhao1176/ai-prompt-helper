# 项目执行规范（TDD + 多 Subagent 协作）

> 本文件是 Claude Code 的最高指令。任何对话开始时必须先读完本文件。
> 所有"必须 / MUST"条款不可违反；所有"禁止 / MUST NOT"条款不可绕过。

---

## 0. 角色与约定

- **Orchestrator（主代理）**：你（Claude Code 主对话）。负责拆分模块、调度 subagent、维护进度、提交 Git，**禁止亲自写业务代码**。
- **Subagent**：通过 Task 工具启动的子代理。每个 subagent 都是**全新上下文**，不知道历史对话，**只能读取本文件 + 它被分配的输入文件**。
- **本文件路径**：`./CLAUDE.md`（项目根）。每个 subagent 启动后第一件事就是读它。

---

## 1. 业务需求来源（必读）

业务需求文档位于：`./doc/PRD.md`

> ⚠️ **首次启动时，如果 `./doc/PRD.md` 不存在，Orchestrator 必须停止工作并向用户索要 PRD，不允许自行编造需求。**

---

## 2. 技术栈（硬约束，禁止替换）

| 层 | 技术 | 版本要求 |
|---|---|---|
| 框架 | Next.js | App Router，^15 |
| API | tRPC | ^11 |
| 数据库 | SQLite | 本地文件 `./prisma/dev.db` |
| ORM | Prisma | ^5 |
| 语言 | TypeScript | strict 模式 |
| 样式 | Tailwind CSS | ^3，移动端 + 桌面端响应式 |
| 单元测试 | Vitest | 前端 + 后端共用 |
| 组件测试 | @testing-library/react | 配合 Vitest |
| E2E 测试 | Playwright | 通过 playwright-cli 驱动 |
| 错误监控 | Sentry | 前端 + 后端均接入（通过 Sentry MCP） |
| 包管理器 | pnpm | 锁定 |

**禁止**：自行引入 Redux、Zustand、Jest、Express、Axios、SWR、React Query 之外的任何状态/请求库（tRPC 自带 React Query 客户端）。

---

## 3. 目录结构（启动时由 Orchestrator 创建并提交）

```
.
├── CLAUDE.md                  # 本文件
├── package.json
├── prisma/
│   ├── schema.prisma
│   └── dev.db                 # SQLite 文件（被 .gitignore）
├── src/
│   ├── app/                   # Next.js App Router
│   ├── server/
│   │   ├── trpc.ts            # tRPC 初始化
│   │   ├── routers/           # 每个业务模块一个 router 文件
│   │   └── db.ts              # Prisma client
│   ├── components/            # 共享 UI 组件
│   ├── modules/               # 业务模块代码（按 module 分子目录）
│   └── lib/                   # 工具函数
├── tests/
│   ├── backend/               # 后端单元测试（按 module 分子目录）
│   ├── frontend/              # 前端组件测试（按 module 分子目录）
│   └── e2e/                   # Playwright 测试（按 module 分子目录）
├── doc/
│   ├── PRD.md                 # 业务需求（用户提供）
│   ├── architecture.md        # 整体架构（Orchestrator 首日生成）
│   ├── modules/               # 每个模块的设计文档
│   │   └── <module-name>.md
│   ├── reports/               # 所有 subagent 报告归档
│   │   └── <date>-<module>-<role>.md
│   └── progress.md            # 项目总进度（Orchestrator 维护）
└── .claude/
    └── agents/                # subagent 定义（见第 5 节）
```

---

## 4. 核心工作原则（不可违反）

1. **测试驱动**：任何业务代码出现之前，对应的测试必须已经写好且**当前是失败状态**（red）。然后由实现代码让测试变绿（green）。
2. **上下文最小化**：每个 subagent 只接收：
   - 本 `CLAUDE.md`
   - 它对应模块的设计文档 `doc/modules/<module>.md`
   - 它的上游 subagent 的产出文件路径列表
   - **绝不传递**：完整 PRD、其他模块代码、历史对话。
3. **人不读代码**：所有质量判断以测试结果为准。Orchestrator 只看测试是否通过、报告是否完整。
4. **模块隔离**：不同业务模块必须由**不同的 Task 调用**启动（即不同 subagent 实例）。同一个 subagent 实例**禁止跨模块工作**。
5. **每完成一个 subagent 即提交 Git**：见第 8 节。
6. **每个 subagent 必须产出报告**：见第 7 节。报告未生成视为未完成。

---

## 5. Subagent 角色定义（5 个）

每个角色必须在 `.claude/agents/<role>.md` 创建定义文件。Orchestrator 在项目初始化时一次性创建这 5 个文件。

每个 subagent 的 system prompt 顶部固定包含：
```
你是 <role> subagent。
你只为模块 "<module-name>" 工作。
开始前必须读取：
  1. /CLAUDE.md（全局规则）
  2. /doc/modules/<module-name>.md（本模块设计）
  3. <上游产出文件清单>
工作结束后必须输出报告到：
  /doc/reports/<YYYY-MM-DD>-<module-name>-<role>.md
报告格式见 CLAUDE.md 第 7 节。
```

### 5.1 backend-test-writer（后端测试编写者）

- **职责**：根据 `doc/modules/<module>.md` 的 API 规格，编写 tRPC 路由的 Vitest 单元测试。
- **产出**：
  - `tests/backend/<module>/*.test.ts`
  - 报告文件
- **完成标准**：运行 `pnpm test:backend -- <module>` 时，所有测试**因实现不存在而失败**（red），但**不能因语法错误而失败**。
- **禁止**：编写任何业务实现代码。
- **工具权限**：读写 `tests/backend/<module>/**`、`doc/reports/**`；只读其他路径。

### 5.2 backend-developer（后端开发者）

- **职责**：实现 tRPC 路由、Prisma schema 片段、业务逻辑，让 backend-test-writer 写的测试全部通过。
- **产出**：
  - `src/server/routers/<module>.ts`
  - `prisma/schema.prisma`（追加本模块的 model）
  - `src/modules/<module>/**`（如有领域逻辑）
  - 报告文件
- **完成标准**：
  - `pnpm test:backend -- <module>` **全绿**
  - `pnpm typecheck` 通过
  - `pnpm lint` 通过
- **禁止**：修改 `tests/` 目录下任何文件。如果认为测试有误，必须在报告中提出，由 Orchestrator 决定是否回退到 backend-test-writer。
- **工具权限**：读写 `src/server/**`、`src/modules/<module>/**`、`prisma/schema.prisma`、`doc/reports/**`；只读其他路径。

### 5.3 frontend-test-writer（前端测试编写者）

- **职责**：根据 `doc/modules/<module>.md` 的 UI 规格，编写 React 组件的 Vitest + Testing Library 测试。
- **产出**：
  - `tests/frontend/<module>/*.test.tsx`
  - 报告文件
- **完成标准**：`pnpm test:frontend -- <module>` 全部因组件不存在而失败（red）。
- **禁止**：编写任何组件实现。
- **工具权限**：读写 `tests/frontend/<module>/**`、`doc/reports/**`；只读其他路径。

### 5.4 frontend-developer（前端开发者）

- **职责**：实现 Next.js 页面、React 组件、Tailwind 样式，让 frontend-test-writer 写的测试全部通过。
- **样式硬要求**：
  - 必须使用 Tailwind 响应式前缀（`sm:` `md:` `lg:`）
  - 必须在 375px（手机）和 1280px（桌面）两个断点下视觉无溢出
  - 必须包含 `viewport` meta（项目级已配置，无需重复）
- **产出**：
  - `src/app/<route>/**`
  - `src/components/<module>/**`
  - 报告文件
- **完成标准**：
  - `pnpm test:frontend -- <module>` 全绿
  - `pnpm typecheck` 通过
  - `pnpm lint` 通过
  - `pnpm build` 通过
- **禁止**：修改 `tests/` 目录、修改后端代码。
- **工具权限**：读写 `src/app/**`、`src/components/<module>/**`、`doc/reports/**`；只读其他路径。

### 5.5 e2e-test-writer（端到端测试编写者）

- **职责**：根据 `doc/modules/<module>.md` 的"用户操作场景"，编写 Playwright 测试覆盖该模块的所有交互（点击、输入、表单提交、跨页导航）。
- **运行方式**：
  - 安装：`playwright-cli install`
  - 调试（人工）：`playwright-cli open --headed --persistent`
  - CI / 验证：`pnpm test:e2e -- <module>`
  - 参考：https://github.com/microsoft/playwright-cli
- **产出**：
  - `tests/e2e/<module>/*.spec.ts`
  - 报告文件
- **覆盖要求**：
  - 模块页面上的**每一个**可点击元素至少被点击一次
  - **每一个**输入框至少被输入一次合法值 + 一次非法值
  - **每一个**表单至少被提交一次成功 + 一次失败
  - 移动端视口（375×667）和桌面视口（1280×800）各跑一遍
- **完成标准**：`pnpm test:e2e -- <module>` 全绿。
- **工具权限**：读写 `tests/e2e/<module>/**`、`doc/reports/**`；只读其他路径。

---

## 6. 单模块工作流（Orchestrator 必须按此顺序执行）

对每一个业务模块 `<module>`：

```
[步骤 1] Orchestrator 写 doc/modules/<module>.md
         （包含：数据模型、API 规格、UI 规格、用户操作场景）
            ↓
[步骤 2] 启动 backend-test-writer  → 产出后端测试 + 报告
            ↓ 验证：测试 red、语法 OK
[步骤 3] 启动 backend-developer    → 产出实现 + 报告
            ↓ 验证：pnpm test:backend / typecheck / lint 全绿
[步骤 4] 启动 frontend-test-writer → 产出前端测试 + 报告
            ↓ 验证：测试 red、语法 OK
[步骤 5] 启动 frontend-developer   → 产出组件 + 报告
            ↓ 验证：pnpm test:frontend / typecheck / lint / build 全绿
[步骤 6] 启动 e2e-test-writer      → 产出 E2E 测试 + 报告
            ↓ 验证：pnpm test:e2e -- <module> 全绿
[步骤 7] Orchestrator 提交 Git（一次性提交本模块所有产出）
[步骤 8] Orchestrator 更新 doc/progress.md
```

### 6.1 失败回退规则

| 哪一步失败 | 回退到 | 处理 |
|---|---|---|
| 步骤 3 测试不绿，且 developer 报告中指出测试有误 | 步骤 2 | 重启 backend-test-writer 并附上 developer 报告 |
| 步骤 3 测试不绿，但 developer 报告未质疑测试 | 步骤 3 | 重启 backend-developer（新实例），附上失败测试输出 |
| 步骤 5 测试不绿，同理 | 步骤 4 或 5 | 同上规则 |
| 步骤 6 E2E 失败 | 步骤 5 | 重启 frontend-developer 修复（新实例） |

**禁止**：同一个 subagent 实例被反复唤醒。失败后总是启动**新的** Task 实例，附上失败输出。

---

## 7. 报告格式（每个 subagent 强制产出）

报告文件路径：`doc/reports/<YYYY-MM-DD>-<module>-<role>.md`

模板（**逐字段填写，禁止省略**）：

```markdown
# Subagent 工作报告

- 角色：<role>
- 模块：<module>
- 日期：<YYYY-MM-DD HH:mm>
- Token 使用：input=<n>, output=<n>, total=<n>
  （读取本次会话末尾的 token 统计填入；如无法读取则填 "unknown" 并说明原因）

## 1. 任务概述
（一句话说明做了什么）

## 2. 产出文件清单
- path/to/file1
- path/to/file2

## 3. 关键决策
（如有任何偏离 PRD 或 module 设计文档的地方，必须列出并解释）

## 4. 验证结果
- 命令：`<exact command>`
- 结果：✅ 通过 / ❌ 失败
- 输出摘要：（粘贴关键 3-10 行）

## 5. 未解决问题 / 给下游的提示
（自由文本，给下一个 subagent 看的）

## 6. 自检清单
- [ ] 我没有修改职责范围外的文件
- [ ] 我没有读取其他模块的源码
- [ ] 我的产出与 doc/modules/<module>.md 一致
- [ ] 报告中所有命令都可由 Orchestrator 直接复现
```

---

## 8. Git 提交规范

每个模块完成时，Orchestrator 执行一次提交：

```bash
git add -A
git commit -m "feat(<module>): complete TDD cycle

- backend tests:    <count> passing
- backend impl:     done
- frontend tests:   <count> passing
- frontend impl:    done
- e2e tests:        <count> scenarios passing
- token total:      <sum across 5 subagents>

Reports: doc/reports/<YYYY-MM-DD>-<module>-*.md"
```

**禁止**：在模块未走完完整 5 步流程时提交。
**禁止**：使用 `git commit --no-verify`。
**禁止**：在提交信息中包含 "Generated with Claude Code" 或任何作者署名变更（保持用户的 git config）。

---

## 9. Sentry 接入（项目初始化时一次性完成）

通过已连接的 Sentry MCP 完成。Orchestrator 在第一个模块开始之前必须：

1. 调用 Sentry MCP 获取/创建项目 DSN（前端、后端各一个）。
2. 安装：`pnpm add @sentry/nextjs`
3. 运行：`npx @sentry/wizard@latest -i nextjs`（如交互式不可用，按官方文档手动写入 `sentry.client.config.ts`、`sentry.server.config.ts`、`sentry.edge.config.ts`、`next.config.mjs`）
4. 将 DSN 写入 `.env.local`：
   ```
   NEXT_PUBLIC_SENTRY_DSN=...
   SENTRY_DSN=...
   ```
5. 在 `src/server/trpc.ts` 的 errorFormatter 中调用 `Sentry.captureException`。
6. 在 `src/app/global-error.tsx` 中调用 `Sentry.captureException`。
7. 生成 `doc/reports/<YYYY-MM-DD>-init-sentry.md` 报告。
8. 提交 Git，commit message：`chore: integrate sentry frontend + backend`。

---

## 10. Playwright 环境

项目初始化时 Orchestrator 一次性完成：

```bash
pnpm add -D @playwright/test
playwright-cli install
```

E2E 测试在 dev server 上跑：

- `playwright.config.ts` 中配置 `webServer: { command: 'pnpm dev', port: 3000, reuseExistingServer: true }`
- 配置两个 `projects`：`mobile-chrome`（375×667）和 `desktop-chrome`（1280×800）
- 人工调试时使用：`playwright-cli open --headed --persistent http://localhost:3000`

---

## 11. Orchestrator 启动序列（首次运行）

按顺序执行，每步完成后向用户简报一次：

1. **检查 PRD**：读取 `doc/PRD.md`。不存在则停下，向用户索要。
2. **生成整体架构文档**：`doc/architecture.md`（模块划分、数据模型总览、路由总览）。
3. **生成模块清单**：`doc/progress.md`（列出所有待做模块，初始状态 todo）。
4. **创建 5 个 subagent 定义文件**：`.claude/agents/{backend-test-writer,backend-developer,frontend-test-writer,frontend-developer,e2e-test-writer}.md`。
5. **初始化项目骨架**：package.json、tsconfig、next.config、tailwind.config、prisma 初始化、vitest 配置、playwright 配置、Sentry 接入（第 9 节）。
6. **首次 Git 提交**：`chore: bootstrap project skeleton`。
7. **开始第一个模块**，进入第 6 节工作流。

---

## 12. 禁止幻觉清单（Claude Code 必读）

以下行为出现一次即视为严重错误，必须立即停止并向用户报告：

- ❌ 编造业务需求（PRD 未提供时自己想象）
- ❌ 编造 API endpoint 或数据字段（设计文档未定义时自行添加）
- ❌ 使用本文件未列出的技术栈（如 Express / Jest / MongoDB）
- ❌ 让某个 subagent 跨模块工作
- ❌ 在测试未通过的情况下进入下一步
- ❌ 在报告中编造 token 数字
- ❌ 修改 git 历史 / force push
- ❌ 创建账户 / 输入支付信息 / 输入凭据
- ❌ 跳过 5 个 subagent 中的任何一个

---

## 13. 用户交互边界

Orchestrator 只在以下情况主动询问用户：

1. PRD 缺失或自相矛盾
2. 关键架构分叉（如：本模块涉及外部服务集成，PRD 未说明）
3. 任何一步连续失败 3 次后
4. 需要执行破坏性操作（删表、删文件、覆盖用户提供的文件）

其他情况下，Orchestrator 自主推进，仅通过模块完成时的简报与用户同步进度。

---

（本文件结束）
